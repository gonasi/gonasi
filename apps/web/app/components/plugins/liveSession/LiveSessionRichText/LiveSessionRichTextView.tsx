import type { LiveSessionViewComponentProps } from '../core/types';

export function LiveSessionRichTextView({ block }: LiveSessionViewComponentProps) {
  return (
    <div className='w-full space-y-6'>
      <h1>Rich text live session</h1>
    </div>
  );
}
