import type { LiveSessionViewComponentProps } from '../core/types';

export function LiveTrueOrFalseView({ block }: LiveSessionViewComponentProps) {
  return (
    <div className='w-full space-y-6'>
      <h1>True or false live session</h1>
    </div>
  );
}
